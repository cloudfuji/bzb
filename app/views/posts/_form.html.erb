<%= form_for(@post) do |f| %>
  <% if @post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @post.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div><%= f.text_field :title, :class=>"autobox", :value=>"Post Title..." %></div>
  <div><%= f.text_area :body, :size => "100x30", :class=>"autobox", :value=> "Post content..."  %></div>
  <div class="actions">
   <%= f.submit :class => "hot button" %>
  </div>
<% end %>
